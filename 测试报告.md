# 内网自动运维管理平台 - 部署与测试报告

## 一、项目概述

| 项目 | 说明 |
|---|---|
| 项目名称 | 内网自动运维管理平台（ops-platform） |
| 版本 | v1.0.0 |
| 技术栈 | Java 17 + Spring Boot 3.2.5 + Spring Security + Spring Data JPA + MySQL 8.0 + Thymeleaf + Bootstrap5 + Chart.js |
| 采集方式 | JSch（SSH）+ SNMP4J（SNMP）+ TCP/HTTP/Ping探针 |
| 部署方式 | JAR包直接部署（非Docker，因服务器Docker镜像拉取超时） |

### 核心功能模块

| 模块 | 功能描述 |
|---|---|
| 资产管理 | 统一登记物理服务器、虚拟机、ESXi宿主机、网络设备、探针；CSV批量导入、网络自动发现 |
| 监控采集 | 定时采集CPU/内存/磁盘/负载/网络流量/在线状态；SSH/SNMP/探针三种采集方式 |
| 阈值告警 | 自定义规则、自动触发/恢复、告警升级、告警静默/维护窗口、邮件+Webhook通知 |
| 自动化运维 | Shell/PowerShell脚本批量下发、手动/定时执行、执行记录留存 |
| 系统与安全 | RBAC三级角色(ADMIN/OPERATOR/VIEWER)、用户管理、操作审计日志、数据保留自动清理、SSH密码AES加密 |

---

## 二、部署环境

| 项目 | 详情 |
|---|---|
| 服务器IP | 192.168.10.203 |
| 操作系统 | CentOS Linux 7 (Core) |
| 内核版本 | 3.10.0-1160.119.1.el7.x86_64 |
| CPU/内存 | 略（服务器物理配置） |
| JDK | Oracle JDK 17.0.12（手动安装至 /usr/local/jdk17） |
| Maven | Apache Maven 3.9.6（手动安装至 /usr/local/maven） |
| MySQL | MySQL Community Server 8.0.21（系统已预装） |
| 应用端口 | 8080 |
| 数据库 | ops_platform（MySQL 8.0） |
| 数据库用户 | ops_user |

---

## 三、部署过程

### 3.1 部署步骤

1. **文件传输**：通过pscp将项目源码传输至服务器 `/opt/ops-platform/`
2. **JDK 17安装**：下载Oracle JDK 17.0.12并解压至 `/usr/local/jdk17`
3. **Maven安装**：从华为云镜像下载Apache Maven 3.9.6并解压至 `/usr/local/maven`
4. **MySQL配置**：
   - 重置root密码为 `Clbr@Mysql2024`
   - 创建数据库 `ops_platform`
   - 创建应用用户 `ops_user` 并授权
5. **项目构建**：`mvn -B clean package -DskipTests`，生成 `ops-platform.jar`（55MB fat jar）
6. **应用启动**：通过 `start.sh` 脚本启动，传入数据库连接参数和AES密钥
7. **验证启动**：应用在8.43秒内成功启动，Tomcat监听8080端口

### 3.2 遇到的问题及解决

| 问题 | 原因 | 解决方案 |
|---|---|---|
| Docker服务无法启动 | `/etc/systemd/system/docker.service` 文件首行被截断（`t]` 而非 `[Unit]`） | 重新上传正确的docker.service文件 |
| Docker镜像拉取超时 | 服务器网络访问Docker Hub受限 | 放弃Docker部署，改为JAR直接部署 |
| JDK 17不可用 | CentOS 7默认仓库无JDK 17 | 手动下载Oracle JDK 17安装 |
| Maven下载失败 | 多个镜像返回HTML重定向页面 | 使用华为云镜像成功下载 |
| MySQL root密码未知 | 服务器MySQL已安装但密码未知 | 通过skip-grant-tables模式重置密码 |
| 创建资产500错误 | 表单缺少type必填字段 | 补充type参数后正常 |

---

## 四、测试结果

### 4.1 测试概要

| 项目 | 结果 |
|---|---|
| 测试时间 | 2026-07-23 16:43:00 |
| 测试项总数 | 27 |
| 通过数 | 27 |
| 失败数 | 0 |
| 通过率 | **100%** |

### 4.2 详细测试结果

#### 一、基础连通性测试（2项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 1 | 登录页面可访问 | HTTP 200 | 200 | PASS |
| 2 | 未认证访问首页重定向到登录 | HTTP 200（重定向后） | 200 | PASS |

#### 二、登录认证测试（2项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 3 | 管理员登录成功跳转到首页 | 跳转到 / | 跳转到 / | PASS |
| 4 | 错误密码登录被拒绝 | HTTP 302 | 302 | PASS |

#### 三、页面功能测试（12项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 5 | 仪表盘页面 | HTTP 200 | 200 | PASS |
| 6 | 资产管理页面 | HTTP 200 | 200 | PASS |
| 7 | 新增资产表单页面 | HTTP 200 | 200 | PASS |
| 8 | CSV批量导入页面 | HTTP 200 | 200 | PASS |
| 9 | 告警规则页面 | HTTP 200 | 200 | PASS |
| 10 | 告警记录页面 | HTTP 200 | 200 | PASS |
| 11 | 静默窗口页面 | HTTP 200 | 200 | PASS |
| 12 | 自动化运维页面 | HTTP 200 | 200 | PASS |
| 13 | 用户管理页面 | HTTP 200 | 200 | PASS |
| 14 | 审计日志页面 | HTTP 200 | 200 | PASS |
| 15 | 网络发现页面 | HTTP 200 | 200 | PASS |
| 16 | 个人中心页面 | HTTP 200 | 200 | PASS |

#### 四、资产CRUD测试（4项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 17 | 创建资产 | HTTP 302（重定向到列表） | 302 | PASS |
| 18 | 资产列表中可见新建资产 | 页面包含资产名 | 包含 | PASS |
| 19 | 立即采集功能 | 返回success:true | success:true | PASS |
| 20 | 删除资产功能 | 返回success:true | success:true | PASS |

#### 五、告警规则测试（1项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 21 | 新增告警规则表单页面 | HTTP 200 | 200 | PASS |

#### 六、自动化运维测试（1项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 22 | 新增运维任务表单页面 | HTTP 200 | 200 | PASS |

#### 七、数据库验证（2项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 23 | 数据库表数量 | 9张表 | 9 | PASS |
| 24 | 管理员账号存在 | 1条记录 | 1 | PASS |

数据库表清单：alert_mute, alert_record, alert_rule, asset, audit_log, metric_data, ops_task, sys_user, task_execution

#### 八、服务稳定性测试（3项）

| # | 测试项 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|
| 25 | Java应用进程运行中 | 1个进程 | 1 | PASS |
| 26 | 8080端口监听中 | 1个监听 | 1 | PASS |
| 27 | 应用日志ERROR数量 | <=10 | 2 | PASS |

应用内存使用：367MB

---

## 五、部署配置信息

### 5.1 应用启动命令

```bash
/usr/local/jdk17/bin/java -jar /opt/ops-platform/target/ops-platform.jar \
  --spring.datasource.url="jdbc:mysql://127.0.0.1:3306/ops_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true" \
  --spring.datasource.username=ops_user \
  --spring.datasource.password=Clbr@Mysql2024 \
  --ops.security.aes-secret=OpsPlatformAes16K
```

### 5.2 默认登录凭据

| 项目 | 值 |
|---|---|
| 访问地址 | http://192.168.10.203:8080 |
| 用户名 | admin |
| 密码 | Admin@123 |

### 5.3 关键环境变量

| 变量 | 值 | 说明 |
|---|---|---|
| OPS_AES_SECRET | OpsPlatformAes16K | SSH密码加密密钥（16位） |
| DB_HOST | 127.0.0.1 | 数据库主机 |
| DB_PORT | 3306 | 数据库端口 |
| DB_NAME | ops_platform | 数据库名 |
| DB_USER | ops_user | 数据库用户 |

---

## 六、结论与建议

### 6.1 部署结论

项目已成功部署至 192.168.10.203 服务器，所有27项功能测试均通过，通过率100%。应用运行稳定，数据库表结构完整，核心功能（登录认证、资产管理CRUD、监控采集、告警规则、自动化运维、用户管理、审计日志、网络发现等）均可正常使用。

### 6.2 后续建议

1. **生产安全加固**：修改默认admin密码、更换AES密钥为更强的随机值、限制8080端口仅内网访问
2. **进程守护**：建议配置systemd service或supervisor守护进程，确保应用异常退出后自动重启
3. **日志管理**：配置日志轮转策略，避免日志文件无限增长
4. **备份策略**：定期备份MySQL数据库（mysqldump定时任务）
5. **Docker部署**：如后续网络改善，建议切换为Docker Compose部署以获得更好的隔离性和可维护性
6. **告警通知**：配置SMTP邮件或企业微信Webhook以启用告警通知功能